# dArchiva Deployment Configuration
# Copy to .env.deploy and fill in your values

# Remote server hostname or IP (required)
DEPLOY_HOST=your-server.example.com

# SSH user for deployment (default: deploy)
DEPLOY_USER=deploy

# Remote deployment path (default: /opt/darchiva)
DEPLOY_PATH=/opt/darchiva

# Git branch to deploy (default: main)
DEPLOY_BRANCH=main

# PM2 app name prefix (default: darchiva)
PM2_APP_NAME=darchiva
